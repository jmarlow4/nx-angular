export const appRoutes: {
  [name: string]: {
    path: string;
    icon: string;
    navLabel: string;
    order: number;
  };
} = {
  cycle: {
    path: 'todo',
    icon: 'list',
    navLabel: 'todo',
    order: 0,
  },
};
