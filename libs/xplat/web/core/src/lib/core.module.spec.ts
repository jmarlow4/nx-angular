describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { ClientCoreModule } from './core.module';

describe('ClientCoreModule', () => {
  it('should work', () => {
    expect(new ClientCoreModule(null)).toBeDefined();
  });
});
*/
